# Stratos tool API keys.
# Get the real, filled-in file from 1Password, drop it into this folder, then ask
# Claude to "load my keys". Do not commit a filled-in copy.
LINKUP_API_KEY=
APIFY_API_KEY=
NETROWS_API_KEY=
