# Secrets — never commit. The real keys come from 1Password as an env file.
*.env
.env
stratos-keys.env
!.env.example
.claude/.credentials.json
**/.credentials.json

# OS / editor noise
.DS_Store
node_modules/
