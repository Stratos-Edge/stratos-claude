# Secrets — never commit. The real startup package (keys) comes via 1Password.
stratos-startup.md
*.env
.claude/settings.local.json
.claude/.credentials.json
**/.credentials.json

# Dependencies (installed locally at setup; not committed)
node_modules/
package-lock.json

# OS / editor noise
.DS_Store
