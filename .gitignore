# Secrets — never commit. The real startup package (keys) comes via 1Password.
stratos-startup.md
*.env
.claude/settings.local.json
.claude/.credentials.json
**/.credentials.json

# OS / editor noise
.DS_Store
node_modules/
