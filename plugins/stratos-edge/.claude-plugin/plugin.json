{
  "name": "stratos-edge",
  "version": "0.1.0",
  "description": "Stratos Edge Claude Code capabilities — skills, commands, and tools.",
  "author": { "name": "Stratos Edge" },
  "mcpServers": {
    "linkup": {
      "type": "http",
      "url": "https://mcp.linkup.so/mcp",
      "headers": { "Authorization": "Bearer ${LINKUP_API_KEY}" }
    },
    "apify": {
      "type": "http",
      "url": "https://mcp.apify.com",
      "headers": { "Authorization": "Bearer ${APIFY_API_KEY}" }
    }
  }
}
